<%= bootstrap_form_for(@vale) do |f| %>
  <% if @vale.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@vale.errors.count, "error") %> prohibited this vale from being saved:</h2>

      <ul>
      <% @vale.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.collection_select(:cuenta_id, Cuenta.all, :id, :nombre) %> <%#Modified for bootstrap_form_for %>
  </div>
  <div class="field">
    <%= f.text_field :responsable %>
  </div>
  <div class="field">
    <%= f.date_select :fechaEmision %>
  </div>
  <!-- <div class="field">
    <%= f.date_select :fechaRendicion %>
  </div> -->  
  <div class="field">
    <%= f.text_field :montoPesos %>
  </div>
  <div class="field">
    <%= f.text_field :montoDolares %>
  </div>
  <div class="field">
    <%= f.text_area :comentario %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
